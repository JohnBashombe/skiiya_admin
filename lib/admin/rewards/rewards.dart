import 'package:flutter/material.dart';

class PriceAndRewards extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Details of Prices and Rewards'),
    );
  }
}
