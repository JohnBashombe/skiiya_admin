import 'package:flutter/material.dart';

class UpdateContact extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Update SkiiYa Contacts Details'),
    );
  }
}
