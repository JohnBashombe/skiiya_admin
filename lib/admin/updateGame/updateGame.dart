import 'package:flutter/material.dart';

class UpdateGameDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Update Game Details'),
    );
  }
}
