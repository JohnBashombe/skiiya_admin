import 'package:flutter/material.dart';

class UserWithdraw extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Withdraw Panel'),
    );
  }
}
